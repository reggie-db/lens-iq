# Databricks Apps runs `npm ci` with NODE_ENV=production, which would skip
# devDependencies. We need them for `npm run build` (tsdown, vite, tsc) to
# succeed on the platform, so flip production off here.
production=false
