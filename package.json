{
  "name": "lens-iq",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Computer vision dashboard for quick serve restaurants - merged pizza detector + IoT monitoring demo on Databricks AppKit",
  "scripts": {
    "dev": "NODE_ENV=development tsx watch server/server.ts",
    "build": "npm run build:server && npm run build:client",
    "build:server": "tsdown --out-dir build --entry.index server/server.ts",
    "build:client": "tsc -b && vite build --config client/vite.config.ts",
    "deploy": "scripts/deploy.sh",
    "start": "node build/index.mjs",
    "typecheck": "tsc -b --noEmit"
  },
  "dependencies": {
    "@databricks/appkit": "^0.35.2",
    "@databricks/appkit-ui": "^0.35.2",
    "@tailwindcss/vite": "^4.1.11",
    "lucide-react": "^0.525.0",
    "marked": "^17.0.6",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "react-router-dom": "^7.9.5",
    "recharts": "^3.1.2",
    "sonner": "^2.0.7",
    "tailwindcss": "^4.1.11",
    "tw-animate-css": "^1.2.5",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^24.3.0",
    "@types/react": "^19.1.8",
    "@types/react-dom": "^19.1.6",
    "@vitejs/plugin-react": "^4.7.0",
    "tsdown": "^0.20.3",
    "tsx": "^4.19.0",
    "typescript": "~5.8.3",
    "vite": "^7.0.5"
  },
  "engines": {
    "node": ">=22"
  }
}
